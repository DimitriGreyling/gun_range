import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xFF4F46E5);
  static const primaryForeground = Color(0xFFFFFFFF);

  // Background
  static const backgroundLight = Color(0xFFF9FAFB);
  static const backgroundDark = Color(0xFF111827);

  // Card
  static const cardLight = Color(0xFFFFFFFF);
  static const cardDark = Color(0xFF1F2937);

  // Muted Foreground
  static const mutedForegroundLight = Color(0xFF6B7280);
  static const mutedForegroundDark = Color(0xFF9CA3AF);

  // Input
  static const inputLight = Color(0xFFE5E7EB);
  static const inputDark = Color(0xFF374151);

  // Foreground
  static const foregroundLight = Color(0xFF1F2937);
  static const foregroundDark = Color(0xFFF9FAFB);

  // Button
  static const buttonRadius = 12.0;
  static const borderRadius = 8.0;
  static const borderRadiusLg = 12.0;
  static const borderRadiusXl = 16.0;
  static const borderRadiusFull = 9999.0;
}
